# 贡献指南 · Contributing

维古登语是一门**个人创造但已成熟**的人造语：它有自己的音系、六格系统、动词词缀链与诗歌体。
因此本项目的贡献原则是——**「加」容易，「改」要慎重**。任何改动都可能波及词典、规范、语料与工具四处。

---

## 一、先判断你要做的是哪一类贡献

| 类型 | 典型内容 | 需要动的位置 | 审核强度 |
| --- | --- | --- | :---: |
| **勘误** | 拼写错误、音标错误、表格错行、错别字 | 源 docx → 重新生成 | 低 |
| **新词** | 提出一个尚无对应词的义项 | `dictionary/`（经 Issue 讨论） | 中 |
| **语料** | 新的诗、书信、小说段落 | `corpus/` | 中 |
| **规范修改** | 改音系、改格系统、增删词缀 | `docs/` + 词典 + 语料同步 | **高** |
| **工具** | 词典网页、校验脚本、转换器 | `tools/` | 低 |

---

## 二、硬性规则（会被 CI 拒绝的）

1. **不要直接改 `dictionary/ueguden_dict.*`**
   它是 `tools/build_dict.py` 的生成物。改这里 = 下次生成即被覆盖。
   改词请改源文档 `archive/docx/词典第七版.docx`，或提 Issue 由维护者统一改。

2. **不要手改 `docs/*.md`、`corpus/*.md` 的正文**
   它们同样由 `tools/docx2md.py` 从 docx 生成。要改内容就改源 docx 后重新生成；
   要改**排版**就改 `tools/docx2md.py`。唯一的例外是各目录的 `README.md` 与手写文档
   （如 `docs/14-errata.md` 的源文件 `勘误记录 20260905.md`）。

3. **音标只写标准 IPA**
   音标内不得出现钝音符 `à` `ò`（重音一律写 `ˈ`），不得出现 `ç ĉ ĝ ŭ` 等字母。
   词形则相反：必须只用《正字法规范》字母表内的字符（词缀条目可用连字符，专名可首字母大写）。

4. **诗歌体与常规体不得混用**
   同一篇文本只能属于一个体裁。诗歌体（42 位变格 / 21 位变位、取消送气、节律重音）
   仅用于诗与史诗；其余一律常规体（6 格 × 单复数）。

5. **清除世界语残留**
   维古登语没有冠词。出现 `la`、`kaj`、`ĉu`、独立助动词或 SVO 语序，一律视为待修问题，
   修正方法见 [`docs/15-rewrite-spec-v2.md`](docs/15-rewrite-spec-v2.md)。

---

## 三、提交前自检

```bash
python tools/validate.py            # 必须 0 错误
python tools/validate.py --corpus   # 提交语料时一并跑，看世界语残留与未收录词
```

`validate.py` 的 0 错误是合并的**最低门槛**，不是唯一标准。
它检查的是机械层面的合法性；语言是否「像维古登语」仍需人工判断。

---

## 四、提新词的格式

在 Issue 里按这个模板写，缺项会被要求补齐：

```yaml
词形:      （按正字法，不得与现有 4367 条重复）
音标:      /…/（IPA，含重音符 ˈ）
词类:      名 / 动 / 形 / 副 / 连词 / 介词 / 助词 / 叹词 / 前缀 / 后缀 / 分词
释义:      （中文，简洁）
词源:      （原生词根 / 源自某语言 / 某词 + 某词缀）
例句:      （维古登语句 + 中文对照，须符合 SOV 与格系统）
理由:      （为什么现有词库无法表达这个义项）
```

**优先考虑派生而非造新词根**：先看 `docs/04-morphology.md` 的派生表
（`-an` `-er` `-anto` `-es` `-ion` `-il` `-lok` `-i` `-la` `-bela` `-itá` `-li` `-ment` `-if` `-en` `-ize`），
能派生出来的义项不新造词根。

---

## 五、提交信息规范

用带前缀的祈使句，一行说清改了什么：

```
docs: 补充方位格与工具格的辨析示例
dict: 新增 12 条天文学词汇（DICT_REV 7 → 8）
corpus: 添加秋日挽歌（诗歌体）
tools: validate.py 支持检测钝音符混入音标
fix: 修正 12-tutorial.md 数词表的列数错位
chore: 全量重跑 docx2md 与 build_dict
```

---

## 六、许可证与署名

提交即表示你同意：代码部分按 [MIT](LICENSE-MIT) 授权，
文档、词典与语料部分按 [CC BY-SA 4.0](LICENSE) 授权。

---

## 七、行为准则

参与本项目即表示你同意遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。
对人造语的设计取向可以有分歧，对人不可以。
